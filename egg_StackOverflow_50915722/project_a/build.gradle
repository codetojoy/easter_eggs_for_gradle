
dependencies {
    // change this to 'api' and the Guava jar will appear on the compile classpath
    // that is, in ~/project_b, run `gradle dependencies` and view output

    implementation 'com.google.guava:guava:23.0'
}
